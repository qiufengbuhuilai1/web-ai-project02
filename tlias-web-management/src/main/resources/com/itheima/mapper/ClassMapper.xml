<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itheima.mapper.ClassMapper">

    <select id="list" resultType="com.itheima.pojo.Clazz">
        select c.*, e.name masterName ,
        (case
        when Date(now()) &gt; c.end_date then '已结课'
        when Date(now()) &lt; c.begin_date then '未开班'
        else '在读中' end) status
        from clazz c left join emp e on c.master_id=e.id
        <where>
            <if test="name!=null and name!=''">
                c.name like concat('%',#{name},'%')
            </if>
            <if test="begin!=null and end!=null">
                and c.end_date between #{begin} and #{end}
            </if>
        </where>
    </select>
</mapper>